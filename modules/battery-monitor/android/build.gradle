apply plugin: 'com.android.library'
apply plugin: 'kotlin-android'

group = 'expo.modules.batterymonitor'
version = '1.0.0'

android {
  namespace "expo.modules.batterymonitor"
  compileSdkVersion 34

  defaultConfig {
    minSdkVersion 24
    targetSdkVersion 34
  }
}

dependencies {
  implementation project(':expo-modules-core')
}
